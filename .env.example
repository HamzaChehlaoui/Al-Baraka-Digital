# ==============================================
# Al-Baraka Digital - Environment Variables
# ==============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control!

# ----------------------------------------------
# Database Configuration
# ----------------------------------------------

# MySQL Root Password (for database initialization)
MYSQL_ROOT_PASSWORD=rootpassword

# Database Name
MYSQL_DATABASE=albarakadb

# Database User (application user)
DB_USER=albaraka

# Database Password (application user password)
DB_PASSWORD=albaraka123

# ----------------------------------------------
# JWT Security Configuration
# ----------------------------------------------

# JWT Secret Key (MUST be at least 256 bits / 32 characters)
# IMPORTANT: Change this to a strong random value in production!
# You can generate a secure key using: openssl rand -base64 32
JWT_SECRET=al-baraka-digital-secret-key-2024-very-secure-jwt-token-minimum-256-bits

# ----------------------------------------------
# Optional Configuration
# ----------------------------------------------

# Server Port (default: 8080)
# SERVER_PORT=8080

# Upload Directory (default: /app/uploads in container)
# UPLOAD_PATH=/app/uploads

# JVM Memory Settings (default: -Xmx512m -Xms256m)
# JAVA_OPTS=-Xmx1g -Xms512m
